<Window x:Class="IdeaNest.Views.WorkspaceHostPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:IdeaNest.Views"
        Title="WorkspaceView ホスト表示テスト"
        Width="980" Height="640" MinWidth="780" MinHeight="480"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource AppBackgroundBrush}">
    <DockPanel>
        <Border DockPanel.Dock="Top" Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1"
                Padding="12,8">
            <DockPanel>
                <TextBlock DockPanel.Dock="Right" Text="{Binding DirtyStatus}"
                           Foreground="{StaticResource TextSecondaryBrush}" />
                <TextBlock Text="外部ホスト想定: Workspaceメニューなし / ファイル保存・自動保存なし"
                           Foreground="{StaticResource TextSecondaryBrush}" />
            </DockPanel>
        </Border>
        <views:IdeaNestWorkspaceView x:Name="WorkspaceView"
                                     DataContext="{Binding Workspace}"
                                     ShowMenu="False" />
    </DockPanel>
</Window>
