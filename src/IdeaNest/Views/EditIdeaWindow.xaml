<Window x:Class="IdeaNest.Views.EditIdeaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="アイデア編集"
        Width="560" Height="560"
        WindowStartupLocation="CenterOwner"
        Background="#FAFAFA">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="タイトル" FontWeight="Bold" Margin="0,0,0,2" />
        <TextBox Grid.Row="1" Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

        <TextBox Grid.Row="2" Text="{Binding Body, UpdateSourceTrigger=PropertyChanged}"
                 AcceptsReturn="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 MinHeight="200" Margin="0,0,0,8" />

        <TextBlock Grid.Row="3" Text="タグ (カンマ区切り)" FontWeight="Bold" Margin="0,0,0,2" />
        <TextBox Grid.Row="4" Text="{Binding TagsText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

        <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="0,0,0,4">
            <TextBlock Text="色:" VerticalAlignment="Center" Margin="0,0,8,0" />
            <ComboBox Width="120"
                      ItemsSource="{Binding AvailableColors}"
                      SelectedItem="{Binding Color}" />
        </StackPanel>

        <StackPanel Grid.Row="6" Orientation="Horizontal" Margin="0,0,0,8">
            <CheckBox Content="ピン留め" IsChecked="{Binding IsPinned}" Margin="0,0,16,0" />
            <CheckBox Content="アーカイブ" IsChecked="{Binding IsArchived}" />
        </StackPanel>

        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="キャンセル" Width="100" Margin="0,0,8,0" IsCancel="True" Click="OnCancel" />
            <Button Content="OK" Width="100" IsDefault="True" Click="OnOk" />
        </StackPanel>
    </Grid>
</Window>
