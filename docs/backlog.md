# バックログ (v0.3.0 以降の候補)

優先度はあくまで現時点のメモです。実装着手前に再判断します。

## タグ管理改善 (v0.2.0 で一部実装済み)

- ~~タグの一覧画面でのリネーム / マージ / 削除~~ → v0.2.0 で実装
- ~~未使用タグの自動クリーンアップ~~ → v0.2.0 でカード算出により自動化済み
- タグの色付け
- ハイフン / 半角全角の正規化 (v0.2.0 では基本正規化のみ)

## 色フィルタ (v0.2.1 で実装)

- ~~色チップで色による絞り込みができる~~ → v0.2.1 で実装 (検索バー直下に 8 色チップ + 「(すべて)」)
- ~~色 × タグ × 検索 × アーカイブ の AND 条件~~ → v0.2.1 で実装
- 複数色の同時選択 (OR)
- 色ごとの表示名カスタマイズ
- ユーザー定義カラー (パレット拡張)

## カード並べ替え

- 作成日時順 / タイトル順 への切替
- 手動並べ替え (ドラッグ)
- ピン留めの中での並べ替え

## テキストエクスポート

- v0.2.2 で実装した Markdown 風エクスポートで代替可能。区切り行 (`---`) も含まれる。
- プレーンテキスト専用の出力形式が必要な場合は別途検討。

## Markdown 風エクスポート / クリップボードコピー (v0.2.2–v0.2.3 で実装)

- ~~1 カード = `## タイトル` + 本文 + タグ行 の形式~~ → v0.2.2 で実装
- ~~現在の表示条件 (検索 / タグ / 色 / アーカイブ) を反映~~ → v0.2.2 で実装
- ~~UTF-8 BOM なし / デフォルトファイル名 `ideanest_export_yyyyMMdd_HHmm.md`~~ → v0.2.2 で実装
- ~~クリップボードへのコピー (ファイル保存を伴わない書き出し)~~ → v0.2.3 で実装
- エクスポートテンプレートのカスタマイズ
- カード単位の個別ファイル分割

## NoteNest 向けエクスポート (v0.3.0 で実装、v0.3.1 で更新)

- ~~テキスト形式での NoteNest 向けエクスポート~~ → v0.3.0 で実装 (ファイル出力 + クリップボードコピー)
- ~~`[NOTE]` / `[TODO]` マーカー付与~~ → v0.3.0 で実装
- ~~`[NOTE]` / `[TODO]` / メタ情報の出力有無をオプションで選択~~ → v0.3.1 で実装
- 出力オプションのプレビュー (v0.3.1 では未実装)
- 出力マーカー文言の選択式カスタマイズ
- タグを NoteNest のタグへマッピングした出力
- NoteNest 側でのインポート機能 (v0.3.x では対象外; NoteNest 側の実装が必要)

## 重複アイデア検出

- 本文の n-gram 類似度で重複候補を提示
- ピン留め扱いではなく「同一視」フラグを付ける

## ランダム表示

- 「思考の壁打ち」用に 1 枚だけランダムに表示する画面
- アーカイブ済みを含めるかは選べる

## 今日のアイデア表示

- 更新日が今日 / 直近 N 日のものだけ抽出するクイックフィルタ

## UI 改善

- ダークモード
- カードのサイズ切替 (大 / 中 / 小)
- 一覧と並んでカードの詳細プレビューを表示するスプリットビュー

## カード選択モデル

現状の実装には「選択中カード」という UI 概念が無いため、選択前提のショートカット
(`Del` で削除、`Ctrl+P` でピン留め切替) は v0.1.3 でも見送った。
将来カード選択モデルを導入する場合、以下を併せて検討する。

- カードクリックで選択状態 (薄い枠線 / 影の強調) を持つ
- キーボードフォーカスをカード一覧内で移動できる (`Tab` / 矢印キー)
- `Del`: 選択中カードを削除
- `Ctrl+P`: 選択中カードのピン留め切替
- `Enter`: 選択中カードを編集ダイアログで開く
- `Ctrl+C`: 選択中カードの Markdown コピー (v0.2.3 で `Ctrl+Shift+C` として一括コピーを先に実装済み)

## ショートカットキー追加

- 既に実装済み: `Ctrl+N` / `Ctrl+O` / `Ctrl+S` / `Ctrl+Shift+S` /
  `Ctrl+Shift+N` / `Ctrl+Shift+C` / `Ctrl+F` / 検索欄での `Esc`
- 未対応 (カード選択モデル導入後): `Del`, `Ctrl+P`, カード上での `Enter`
- `Ctrl+D` (複製) など補助系

## その他検討事項

- 自動保存 (n 秒ごと / 編集後 n 秒)
- `.ideanest` 形式のスキーマバージョン管理
- マイグレーション戦略 (v0.1.0 → v0.2.0)
- 検索のあいまい一致 (かな / カナ / 全半角)
